# 1. Базовое окружение
FROM node:18

# 2. Рабочая директория внутри контейнера
WORKDIR /app

# 3. Копируем только package.json и lock-файл (быстрое кеширование зависимостей)
COPY package*.json ./

# 4. Устанавливаем зависимости
RUN npm install

# 5. Копируем весь исходный код внутрь контейнера
COPY . .

# 6. Указываем порт, который будет слушать контейнер
EXPOSE 5000

# 7. Команда запуска приложения
CMD ["node", "src/index.js"]